projection implementation in class zbp_gym_c_user_admin unique;
strict ( 2 );
use draft;

define behavior for ZGYM_C_USER_ADMIN
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  action createMemType;
}